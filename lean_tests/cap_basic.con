// A function with(File) can call another function with(File)
fn read_line() with(File) -> Int {
    return 42;
}

fn read_all() with(File) -> Int {
    let x: Int = read_line();
    return x;
}

fn main() -> Int {
    return 0;
}
