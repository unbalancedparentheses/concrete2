// Test: closure with capability used in pure context
fn needs_cap() with(Console) -> Int {
    return 1;
}

fn pure_fn() -> Int {
    let f: fn() -> Int = fn() -> Int { return needs_cap(); };
    return f();
}

fn main() -> Int {
    return pure_fn();
}
