// Test closure with no captures (pure function wrapped in closure)
// Expected: 100

fn apply(f: fn(Int) -> Int, x: Int) -> Int {
    return f(x);
}

fn main!() -> Int {
    let result: Int = apply(fn(x: Int) -> Int { return x * 2; }, 50);
    return result;
}
