// Trusted fn can do pointer cast at runtime without crash
trusted fn reinterpret(p: *const i32) -> *const u8 {
    return p as *const u8;
}

fn main() with(Unsafe) -> i32 {
    let x: i32 = 42;
    let p: *const i32 = &x as *const i32;
    let q: *const u8 = reinterpret(p);
    return 0;
}
