// Test basic closure, no captures
// Expected: 42

fn apply(f: fn(Int) -> Int, x: Int) -> Int {
    return f(x);
}

fn main!() -> Int {
    let result: Int = apply(fn(x: Int) -> Int { return x * 2; }, 21);
    return result;
}
