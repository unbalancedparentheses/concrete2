// Test: identity closure, pass through
// Expected: 42
fn apply(f: fn(Int) -> Int, x: Int) -> Int {
    return f(x);
}

fn main!() -> Int {
    let id: fn(Int) -> Int = fn(x: Int) -> Int { return x; };
    return apply(id, 42);
}
