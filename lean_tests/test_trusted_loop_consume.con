// Test: trusted fn can consume linear values inside a loop

extern fn malloc(size: u64) -> *mut u8;
extern fn free(ptr: *mut u8);

struct Buf {
    ptr: *mut u8,
    len: u64,
}

trusted impl Buf {
    fn free_entries(self) with(Alloc, Unsafe) {
        let mut i: u64 = 0;
        while i < self.len {
            // Consuming a value inside a loop is OK in trusted code
            let p: *mut u8 = self.ptr + i;
            let val: u8 = *p;
            i = i + 1;
        }
        free(self.ptr);
    }
}

fn main() with(Alloc, Unsafe) -> i32 {
    let p: *mut u8 = malloc(4);
    let b: Buf = Buf { ptr: p, len: 4 };
    b.free_entries();
    return 0;
}
