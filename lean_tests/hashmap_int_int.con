// Test: HashMap<Int, Int> insert and get
fn extract_or(opt: Option<Int>, default: Int) -> Int {
    match opt {
        Option#Some { value } => { return value; },
        Option#None => { return default; },
    }
}

fn main!() -> Int {
    let mut map: HashMap<Int, Int> = map_new::<Int, Int>();
    map_insert(&mut map, 1, 100);
    map_insert(&mut map, 2, 200);
    let v1: Option<Int> = map_get(&map, 1);
    let len: Int = map_len(&map);
    let result: Int = extract_or(v1, 0) + len;
    map_free(map);
    return result;
}
