// Test: main!() calling multiple effectful functions
// Expected: 0
fn print_stuff() with(Console) {
    // uses Console capability
}

fn read_env() with(Env) {
    // uses Env capability
}

fn main!() -> Int {
    print_stuff();
    read_env();
    return 0;
}
