// Test returning a value computed from nested closure application
// Expected: 15 (5 + 10)

fn apply(f: fn(Int) -> Int, x: Int) -> Int {
    return f(x);
}

fn main!() -> Int {
    let offset: Int = 10;
    let result: Int = apply(fn(x: Int) -> Int { return x + offset; }, 5);
    return result;
}
