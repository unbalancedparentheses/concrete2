// Test: function taking and returning closures (higher-order)
// Expected: 15
fn apply(f: fn(Int) -> Int, x: Int) -> Int {
    return f(x);
}

fn main!() -> Int {
    let add5: fn(Int) -> Int = fn(x: Int) -> Int { return x + 5; };
    let result: Int = apply(add5, 10);
    return result;
}
