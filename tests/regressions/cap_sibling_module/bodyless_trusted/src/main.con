mod bodyless_trusted {
    mod decls;
    import decls.{my_sizeof};

    // Declares no Unsafe. A trusted body-less declaration must cost none.
    fn measure() -> u64 {
        return my_sizeof::<i64>();
    }

    fn main() -> i64 {
        return measure() as i64;
    }
}
